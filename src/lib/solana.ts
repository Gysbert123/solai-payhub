export async function getTokenBalance(wallet: string, mint: string): Promise<number> {
  // Placeholder - implement with @solana/web3.js later
  return 0;
}

export async function getCurrentPrice(mint: string): Promise<number> {
  // Placeholder - implement with Jupiter API later
  return 1.0;
}